apiVersion: v1
kind: Service
metadata:
  name: {{ include "fitview.fullname" . }}-frontend
  labels:
    {{- include "fitview.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fitview.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
